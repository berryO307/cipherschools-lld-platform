# Base URL (including /api) of the backend API.
# - Local dev: leave unset — defaults to http://localhost:4000/api
# - Production (Vercel): set to your deployed Railway backend, e.g.
#   VITE_API_URL=https://your-api.up.railway.app/api
VITE_API_URL=
